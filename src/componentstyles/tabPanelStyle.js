import {
    warningColor,
    primaryColor,
    dangerColor,
    successColor,
    infoColor,
    roseColor,
    grayColor,
    defaultFont
} from "../shared/material-kit-pro-react.js";

const tabPanelStyle = {
    warning: {
        color: warningColor[0]
    }
}

export default tabPanelStyle;